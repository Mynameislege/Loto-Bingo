# Behave like npm/yarn — hoist ALL packages to root node_modules.
# Required for Expo + Metro in a pnpm monorepo: without this, Metro follows
# pnpm symlinks into the virtual store and loads mismatched internal deps
# (e.g. getDevServer not a function, PlatformConstants not found).
shamefully-hoist=true
